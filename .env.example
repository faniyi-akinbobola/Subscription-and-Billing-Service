# =================================================================
# SUBSCRIPTION & BILLING SERVICE - ENVIRONMENT CONFIGURATION
# =================================================================

# Application Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_secure_password_here
DB_NAME=subscription_db

# PgAdmin Configuration
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=your_pgadmin_password_here
PGADMIN_HOST_PORT=5050
PGADMIN_CONTAINER_PORT=80

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379

# Docker Images
POSTGRES_IMAGE=postgres:15-alpine
REDIS_IMAGE=redis:7-alpine
PGADMIN_IMAGE=dpage/pgadmin4:latest

# =================================================================
# STRIPE CONFIGURATION (TEST KEYS ONLY - NEVER USE LIVE KEYS!)
# =================================================================
# Get your test keys from: https://dashboard.stripe.com/test/apikeys

# Test Secret Key (starts with sk_test_)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Test Publishable Key (starts with pk_test_)
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# Webhook Secret (starts with whsec_)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# =================================================================
# EMAIL CONFIGURATION
# =================================================================

# SMTP Email Settings (Gmail Configuration)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your_gmail_app_specific_password_here
EMAIL_FROM=your-email@gmail.com
EMAIL_SECURE=false

# Alternative: SendGrid Configuration (Uncomment to use)
# SENDGRID_API_KEY=your_sendgrid_api_key_here

# =================================================================
# JWT CONFIGURATION (Optional - Add if implementing custom JWT secrets)
# =================================================================
# JWT_SECRET=your_jwt_secret_here_minimum_32_characters
# JWT_EXPIRES_IN=86400

# =================================================================
# RATE LIMITING CONFIGURATION (Optional)
# =================================================================
# THROTTLE_TTL=60
# THROTTLE_LIMIT=10

# =================================================================
# PRODUCTION NOTES:
# =================================================================
# 1. Never commit the actual .env file to version control
# 2. Use production-grade passwords for production deployments
# 3. Replace Stripe test keys with live keys for production (pk_live_, sk_live_)
# 4. Use proper SSL certificates for production email SMTP
# 5. Consider using environment-specific secrets management (AWS Secrets Manager, Azure Key Vault, etc.)